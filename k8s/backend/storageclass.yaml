# apiVersion: storage.k8s.io/v1
# kind: StorageClass
# metadata:
#   name: gp3
# provisioner: ebs.csi.aws.com
# volumeBindingMode: WaitForFirstConsumer
# parameters:
#   type: gp3
#   fsType: ext4


#    aws eks describe-addon `
# >>   --cluster-name eks-cluster-terraform `
# >>   --addon-name aws-ebs-csi-driver `
# >>   --region us-east-1 --no-cli-pager


# STEP1 Create EBS CSI DRIVER


# eksctl create iamserviceaccount `
#   --name ebs-csi-controller-sa `
#   --namespace kube-system `
#   --cluster eks-cluster-terraform `
#   --region us-east-1 `
#   --attach-policy-arn arn:aws:iam::aws:policy/service-role/AmazonEBSCSIDriverPolicy `
#   --approve `
#   --role-name AmazonEKS_EBS_CSI_DriverRole `
#   --override-existing-serviceaccounts


# STEP2 
# aws eks describe-addon `
#   --cluster-name eks-cluster-terraform `
#   --addon-name aws-ebs-csi-driver `
#   --region us-east-1 `
#   --no-cli-pager


#Step3 if add on show still in cretaing then 

# aws eks delete-addon `
#   --cluster-name eks-cluster-terraform `
#   --addon-name aws-ebs-csi-driver `
#   --region us-east-1 `
#   --no-cli-pager


# STEP4   create again ebs adds on after delete-addon 

# aws eks create-addon `
#   --cluster-name eks-cluster-terraform `
#   --addon-name aws-ebs-csi-driver `
#   --service-account-role-arn arn:aws:iam::210190732631:role/AmazonEKS_EBS_CSI_DriverRole `
#   --resolve-conflicts OVERWRITE `
#   --region us-east-1 `
#   --no-cli-pager

# # step5 

# kubectl get pods -n kube-system 
# kubectl get csidriver


# Step 6 — Create gp3 StorageClass   kubectl apply -f storageclass.yaml

# apiVersion: storage.k8s.io/v1
# kind: StorageClass
# metadata:
#   name: gp3
# provisioner: ebs.csi.aws.com
# volumeBindingMode: WaitForFirstConsumer
# parameters:
#   type: gp3
#   fsType: ext4


# step7 apply statefulset , service.yaml and configmap after above apply  steps




# Step8 to delete iamserviceaccount

# eksctl delete iamserviceaccount `
#   --name ebs-csi-controller-sa `
#   --namespace kube-system `
#   --cluster eks-cluster-terraform `
#   --region us-east-1


# step9 to delete storage class
# kubectl delete storageclass gp3