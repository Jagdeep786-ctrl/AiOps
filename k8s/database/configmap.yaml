apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-init-scripts
  namespace: shopping
data:
  10-create-databases.sql: |
    CREATE DATABASE auth_db;
    CREATE DATABASE products_db;
    CREATE DATABASE orders_db;
    CREATE DATABASE users_db;
  
  20-init-schema.sql: |
    -- Example: Create tables in auth_db
    \c auth_db
    CREATE TABLE IF NOT EXISTS users (
      id SERIAL PRIMARY KEY,
      name VARCHAR(100),
      email VARCHAR(100) UNIQUE
    );


  quick-seed.sql: |
    -- Seed data for demo
    \c auth_db
    INSERT INTO users(name, email) VALUES('Alice','alice@example.com'),('Bob','bob@example.com');


#kubectl delete configmap postgres-init-scripts -n shopping

# kubectl create configmap postgres-init-scripts --from-file=./init -n shopping
# kubectl delete statefulset boutique-postgres -n shopping
# kubectl delete pvc postgres-data-v2-boutique-postgres-0 -n shopping
# # kubectl get pvc -n shopping
# kubectl apply -f service.yaml -n shopping
# kubectl apply -f statefulset.yaml -n shopping

# kubectl logs boutique-postgres-0 -n shopping